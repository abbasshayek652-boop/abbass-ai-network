{
  "name": "mother-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd app/frontend && npm run dev",
    "dev:backend": "cd app/backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "run-p dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
